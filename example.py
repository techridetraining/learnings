 x = a('u',['s','u','d','h','a'])
#  newarr =b(names.typecode,(a for a in names))

#  for u in newarr:
#      print (u)

# from array import array

# names = array('u', ['s', 'u', 'd', 'h', 'a'])

# Creating a new array using list comprehension
# newarr = array(names.typecode, (a for a in names))

# # Printing elements
# for u in newarr:
#     print(u)


# from array import array

# names = array('u', ['s', 'u', 'd', 'h', 'a'])

# # Creating a new array using list comprehension
# newarr = array(names.typecode, (a for a in names))
# print(newarr)

#  Printing elements
# for u in newarr:
# print(u)